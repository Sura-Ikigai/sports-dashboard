# .claude/CANON-VERSION — Dev-System canon provenance (lockfile)
#
# Records which factory canon this project was stamped from, and last reconciled to.
# Baseline for /reconcile-canon: lets it tell "my project drifted" apart from
# "canon advanced after I stamped" (SYSTEM.md §1).
#
# Factory: ~/Desktop/LabRoomV2.0/Client Projects/Dev-System
#          THE ONLY authoritative factory. `Sports/Dev-System/` in this workspace is a stale clone
#          frozen at bd58a22 (6 commits behind) — see NOT THE FACTORY at the bottom of this file.

stamped-from:    c513a528eab6546559e518373a9691965b9b7e2a
last-reconciled: 26b360f
stamped-date:    2026-08-09
last-reconciled-date: 2026-08-12
reconciled-tree-verified: 2026-08-14   # the date the TREE was actually made to match the line above

# Canon contributions promoted FROM this project (all on Dev-System/main, so /reconcile-canon reads
# them as canon rather than project-local drift):
#   8398baa  stacks/nextjs-fastapi-postgres.md — authored at instantiation; canon had Supabase
#            variants only, and this project runs plain Postgres with no RLS.
#   be5f6dc  checks/lib/tracker.mjs — review-ledger-current currency rule (F-018).
#   6f49f29  checks/ — restore verdict enforcement on DONE; scope only currency (F-019).
#   d8b7a8a  checks/ — fail closed on unrecognized task status (F-025); JSDoc scope (F-024).
#   c513a52  checks/ — anchor task-header match (F-035); derive knownUngated (F-036).
#   1c52645  checks/ — review currency is per-task, not repo-wide (F-043). Pushed to
#            Dev-System/main 2026-08-10; verified on canon's own vitest ^2.1.0 range (53 tests)
#            and both CLI modes smoke-tested against the canon repo's own tracker.
#   26b360f  templates/ + agents/ + SYSTEM.md — split the tracker template into three files, and
#            make reviewers report rather than write (F-072, F-100, F-101, F-102). Pushed to
#            Dev-System/main 2026-08-12; canon suite green on its own vitest ^2.1.0 range.
# `last-reconciled` above is the most recent of these.
#
# CORRECTED 2026-08-14 (PLAN-v2 T-011). This block previously carried an eighth entry claiming
# `fe27280 checks/ — gate-completeness validates agent CONTENT (F-103)` was "promoted as part of
# 26b360f". IT WAS NOT. `git log -- checks/gate-completeness.mjs` in the factory returns exactly one
# commit, bd58a22, and canon's copy is 49 lines with no `evaluateAgentIntegrity`; this project's is
# 59 lines with it. F-103's fix has always been project-local and is now recorded as such below.
# The entry was written in the same session that authored 26b360f and describes an intent, not an
# act — the same class of defect as F-096 (mis-stated gate evidence) and F-019's "filed in Future
# hardening" that was never filed. This file is the project's provenance lockfile; an unverified
# claim in it is worse than no claim, because every later reconcile reads it as ground truth.
#
# DELIBERATELY HELD PROJECT-LOCAL (not defects — policy that changes how much scrutiny work gets;
# promote after a task with a schema migration and an auth surface has exercised them):
#   D-028  scoped re-reviews by default, reviewer holds the full-review trigger.
#   D-029  LOW findings batch, except "a test does not pin what it claims to pin".
#
# STILL PROJECT-LOCAL, not yet promoted (see docs/IMPLEMENTATION.md, Future hardening):
#   F-021  checks/package.json pins vitest 4.1.10 here and committed checks/package-lock.json;
#          canon still ships an unpinned `vitest: ^2.1.0` and no lockfile, so every project
#          stamped from canon inherits the advisory-bearing 2.x range.
#   F-103  checks/gate-completeness.mjs + checks/lib/manifest.mjs — `evaluateAgentIntegrity`
#          (frontmatter `name:` matches filename; a referenced stack overlay is THIS project's).
#          Local commit fe27280. Canon still existence-checks agent FILENAMES only, so every
#          project stamped from canon can still ship the wrong stack's builder agent — the F-008
#          signature. Scheduled for promotion as PLAN-v2 T-014, which also extends the check with
#          the instruction/tool coherence assertion this project's own agents currently fail.
#
# KNOWN DEFECT IN THE SYNCED CANON (accepted deliberately, 2026-08-14, PLAN-v2 T-012):
#   The 26b360f reviewer agents this project now matches instruct a `tools: Read, Grep, Glob` agent
#   to run `git worktree add`, `git archive`, `pytest` and set `PYTHONPYCACHEPREFIX` — none of which
#   it can execute. Syncing to canon propagates that defect knowingly, because the alternative is a
#   lockfile that lies about the tree. PLAN-v2 T-014 adds the coherence check that catches it and
#   T-019 gives reviewers the worktree isolation the prose assumes. Do not "fix" it locally here:
#   a third divergent copy is the problem this task exists to end.
#
# NOT THE FACTORY:
#   `~/Desktop/LabRoomV2.0/Sports/Dev-System/` is a second clone of the same GitHub remote, frozen
#   at bd58a22 — 6 commits behind, missing the reviewer-contract fix, the tracker split, and
#   templates/{findings,decisions}.template.md. Verified 2026-08-14 to hold NOTHING the parent clone
#   lacks (both carry `canon/self-learning-batch-v1` at ebb1dee, whose tree is identical to the
#   squashed bd58a22), so it is safe to delete. It is retained only until PLAN-v2 T-015's composer
#   makes clone provenance mechanical. Read canon from the Factory path above, never from here.
